#include<stdio.h>
void main()
{
    int i, rev=0;
    printf("Enter the no you want to reverse :");
    scanf("%d", &i);
    while(i!=0)
    {
        rev = rev*10 + i%10;
        i = i / 10;
    }
    printf("The reverse is %d\n", rev);
    return 0;
}
